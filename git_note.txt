--git reset --hard "Mất luôn commit"
--git reset --mixed "Quay về giai đoạn working directory"
--git reset --soft "quay về giai đoạn lưu ở add"
-- git branch "Xem mình đang ở nhánh nào"
-- git add. "add tất cả"
-- git add tên file "add file muốn add"
-- git checkout tên branch "Chuyển tới branch đó"
-- git -D tên branch "Xóa branch"
--git merge tên brach muốn merge vào master "Merge vào branch master"
--git status "xem trạng thái của các file đã commit đã add hay chưa"
-- git diff "xem những thay đổi trong git,sau khi commit sẽ ko thấy nữa"
